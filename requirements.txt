# Pinned to 6.8.0.2: PySide6 6.9+ needs a newer MSVC runtime than the
# Anaconda base Python loads (14.42), which causes
# "DLL load failed while importing QtCore: The specified procedure could not be found".
PySide6==6.8.0.2
# mDNS discovery for the Connect tab (browses the ESP32's _lifeos._udp service).
zeroconf>=0.130
# Bluetooth SPP link: reads the ESP32's paired COM port (bt_receiver.py, and the
# Bluetooth transport in the GUI).
pyserial>=3.5
