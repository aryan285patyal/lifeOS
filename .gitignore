# Virtual environments
.venv/
venv/
env/

# Python caches
__pycache__/
*.py[cod]
*.egg-info/

# Editor / tooling
.claude/
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# Secrets -- real credentials live here (copy from secrets.example.h)
secrets.h

# Runtime state -- the Connect tab's remembered default device (machine-specific)
connect_config.json

# Per-device 6-position accel calibration (scale factors; bias lives on the ESP32)
calibration.json

# Session logs -- one file per GUI run (machine-specific, large, carries the SSID)
log/
