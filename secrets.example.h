// secrets.example.h  --  template for the values the sketch needs.
//
// Copy this file to "secrets.h" and fill in your real values:
//     cp secrets.example.h secrets.h   (or copy it in your editor)
//
// secrets.h is listed in .gitignore and must NEVER be committed.

#pragma once

#define WIFI_SSID     "YOUR_WIFI_SSID"        // WiFi network the ESP32 joins
#define WIFI_PASSWORD "YOUR_WIFI_PASSWORD"    // password for that network
#define PC_IP         "192.168.1.100"         // receiving PC's LAN IP (from ipconfig)
